<section id="portfolio" class="{{ content.field_background_color[0]['#markup'] }}">
    <div class="container">
        <div class="row">
            <div class="col col-md-8 col-md-offset-2 text-center">
                <h2>{{ content.field_title }}</h2>
                <p class="lead">{{ content.field_description }}</p>
            </div>
        </div>
        <div class="grid row">
           {{ content.field_grid_item }}
        </div>
    </div>
</section>

